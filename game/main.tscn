[gd_scene load_steps=5 format=2]

[ext_resource path="res://res/bg.png" type="Texture" id=1]
[ext_resource path="res://ball.tscn" type="PackedScene" id=2]
[ext_resource path="res://floor.tscn" type="PackedScene" id=3]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D

# Declare member variables here. Examples:
# var a: int = 2
# var b: String = \"text\"

# Called when the node enters the scene tree for the first time.


func _ready() -> void:

	#print(w);
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	#var w_size = get_viewport_rect();
	get_input()
	pass


func get_input():
	var click = Input.is_action_just_pressed(\"ml\")
	if click:
		push_cube(get_local_mouse_position())
		print('click')
	#if right:
	#	velocity.x = speed

func push_cube(mouse_pos):
	
	var cube:KinematicBody2D = preload(\"res://cube.tscn\").instance();
	#add_child(cube_inst._init());
	#var s = String(get_tree().get;
	 #.add_child(cube);
	cube.set_position(mouse_pos);
	add_child(cube)
	cube;
	pass
	#print(mouse_pos);
	#add_child();                                                                                                                                                                                                                                                                                                                                                                                                                                                      "

[node name="main" type="Node2D"]
script = SubResource( 1 )

[node name="bg" type="Sprite" parent="."]
position = Vector2( 600, 300 )
scale = Vector2( 0.312663, 0.277778 )
texture = ExtResource( 1 )

[node name="ball" parent="." instance=ExtResource( 2 )]
position = Vector2( 320, 230 )
scale = Vector2( 0.5, 0.5 )

[node name="floor" parent="." instance=ExtResource( 3 )]
position = Vector2( 580, 540 )

[node name="Camera2D" type="Camera2D" parent="."]
visible = false

[node name="RichTextLabel" type="RichTextLabel" parent="."]
margin_left = 501.0
margin_top = 27.0
margin_right = 642.0
margin_bottom = 110.0
custom_colors/default_color = Color( 0, 0, 0, 1 )
text = "WASD
and 
Mouse click"
